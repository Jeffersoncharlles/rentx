import React from 'react';

import {
    Container,
    Title,
} from './styles';

export const ConfirmaButton = () => {
    return (
        <Container>
            <Title>Ok</Title>
         </Container>
    );
}